<!--フラッシュメッセージ-->
<div>
  <%= flash[:notice] %>
</div>
       
<!--メイン--> 
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-4">  
      <%= render 'books/list', booker: Book.new %>
    </div>
    <div class="col-md-7 ml-auto"> 
      <%= @user.name %>
      <%= image_tag @user.get_profile_image(100,100) %>
    </div>
  </div>
</div>

