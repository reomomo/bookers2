<div class="container">
  <div class="row">
    <div class="col-md-4">  
      <%= render 'books/list', booker: Book.new, user: current_user %>
    </div>
    <div class="col-md-8.offset-md-1"> 
      <div>
      <h1>Users</h1>
      
      <table class="table">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th></th>
          </tr>
        </thead>
        <% @users.each do |user| %>
        <tbody>
          <tr>
            <td><%= image_tag user.get_profile_image(100,100) %></td>
            <td>
              <%= user.name %>
            </td>
            <td>
              <%= link_to "Show", user_path(user.id) %>
            </td>
          </tr>
        </tbody>
        <% end %>
      </table>
      </div>
    </div>
  </div>
</div>
